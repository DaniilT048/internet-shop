import type {ReactElement} from "react";

const Shop = ():ReactElement => {
    document.title = "Shop";
    return(
        <div>
            <h1>
                Shop Page
            </h1>
        </div>
    )

}

export default Shop;