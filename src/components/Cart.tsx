import type {ReactElement} from "react";

const Cart = ():ReactElement => {
    document.title = "Cart";
    return(
        <div>
            <h1>
                Cart Page
            </h1>
        </div>
    )

}

export default Cart;